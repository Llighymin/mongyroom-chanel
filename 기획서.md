# 영상 자동화 스튜디오 — 기획서

> 버전 `0.1.0` · 대상 플랫폼 macOS · 작성 기준 2026-08  
> 1인 운영자가 본인 Mac에서 돌리는 로컬 Electron 앱

---

## 1. 한 줄 요약

워크스페이스(채널/브랜드) 단위로 **레퍼런스 수집 → 릴스용 편집 → Instagram 업로드 → 첫 댓글**까지 이어 주는 영상 콘텐츠 자동화 스튜디오.

관리자 개입은 다음과 같다.

1. 레퍼런스에서 영상을 고른다 (**선택**).
2. 편집 옵션을 확인하고 실행한다 (**편집**).
3. 미리보기·계정·캡션을 확인한 뒤 게시한다 (**컨펌**).
4. Claude가 만든 첫 댓글 안을 확정한다 (**댓글**, 이후 단계).

다운로드·변환·업로드 중에는 **실시간 진행률**과 문장형 상태를 보여 준다.

---

## 2. 목표·비목표

### 목표

- 채널별 키워드로 YouTube(주력) · Instagram(보조) 레퍼런스를 주기적으로 모은다.
- Claude로 추천·점수화하고, 선택한 영상을 9:16 릴스 규격으로 편집한다.
- Meta Graph API로 Instagram 릴스에 올리고, 첫 댓글까지 이어 준다.
- API 키·토큰은 맥 키체인 기반으로 암호화해 로컬에만 보관한다.
- 비개발자도 쓸 수 있는 친절한 UI(한글 레이블, 문장형 상태, 진행률).

### 비목표 (초기 범위 밖)

- Windows / Linux 지원
- 멀티 유저·클라우드 동기화·팀 협업
- TikTok·YouTube Shorts 업로드
- 완전 무인(선택·댓글 확정까지 자동) 운영
- 저작권 판정·라이선스 자동 처리 (앱은 경고만 표시)

---

## 3. 사용자·사용 시나리오

| 구분 | 내용 |
|------|------|
| 주 사용자 | 1인 운영자(관리자), 본인 Mac에서 단독 실행 |
| 사용 맥락 | 카페·쇼핑몰·개인 브랜드 등 채널별 릴스 운영 |
| 핵심 시나리오 | 키워드·계정만 연결해 두면, 매일 후보가 쌓이고 고른 영상만 릴스로 올린다 |

---

## 4. 핵심 개념: 워크스페이스

모든 설정·수집·이력은 **워크스페이스(채널/브랜드)** 단위로 분리한다.

| 항목 | 설명 |
|------|------|
| 이름 | 채널 표시명 (예: 홈카페) |
| 수집 키워드 | YouTube·IG 검색어 목록 |
| 수집 주기 | 30분 ~ 하루 1회 |
| 릴스 계정 | IG User ID, 페이지 ID, 액세스 토큰 (복수 가능) |

전역(앱 공통) 설정은 워크스페이스와 별도로 둔다.

- Claude API 키
- YouTube Data API 키
- 메타 앱 ID / 시크릿

---

## 5. 파이프라인

```
[1 수집] → [2 선택] → [3 편집] → [4 컨펌·게시] → [5 댓글]
   ↑ 자동 스케줄      ↑ 관리자         ↑ yt-dlp/FFmpeg   ↑ Meta resumable   ↑ 이후
```

### 5.1 레퍼런스 수집

- 워크스페이스 키워드로 **YouTube + Instagram** 검색 (YouTube 주력, IG 보조).
- Claude가 추천·점수화.
- 설정한 간격마다 자동 스케줄.
- 타인 영상 재업로드 리스크가 있으므로 **저작권/약관 경고**를 표시. 최종 선택은 관리자 몫.

### 5.2 선택 → 편집 → 컨펌 (릴스 만들기)

한 레퍼런스 = 작업(job) 하나. 워크스페이스당 동시 진행 1개.

1. **선택** — 레퍼런스 카드에서 「이 영상으로 만들기」. 저작권 경고 확인.
2. **편집** — 워터마크 옵션 후 실행. `yt-dlp` 다운로드 → FFmpeg 9:16(1080×1920)·최대 90초·선택 워터마크.
3. **컨펌** — 결과 미리보기, 업로드 계정·캡션·피드 공유 여부 확인 후 「릴스 게시」.
4. **게시** — Meta Graph **resumable** 업로드 → 컨테이너 처리 대기 → `media_publish`.

로컬 도구: macOS에 `yt-dlp`, `ffmpeg` 설치 필요 (`brew install yt-dlp ffmpeg`). 없으면 앱이 안내한다.

### 5.3 댓글 업로드 *(이후)*

- Claude가 첫 댓글 **3안** 추천.
- 관리자가 확정 또는 편집 후 게시.

---

## 6. 화면 구성

| 화면 | 역할 |
|------|------|
| 웰컴 | 워크스페이스가 없을 때 첫 채널 생성 유도 |
| 사이드바 | 워크스페이스 목록, 설정, 라이트/다크 테마 |
| 워크스페이스 · 레퍼런스 | 수집/직접 등록 목록·검색·「이 영상으로 만들기」 |
| 워크스페이스 · 릴스 만들기 | 선택 → 편집 → 컨펌·게시 3단 흐름 + 진행률 |
| 워크스페이스 · 채널 설정 | 이름·키워드·수집 주기·릴스 계정 관리 |
| 앱 설정 | Claude / YouTube / 메타 앱 키 저장·연결 테스트 |
| 이력 *(예정)* | 업로드·댓글 이력 조회 |

### UI/UX 원칙

- 비개발자 기준: 기술 용어·영문 API 이름을 기본 화면에 노출하지 않는다.
- 상태 메시지는 문장형으로 (“영상을 내려받는 중이에요”).
- 오래 걸리는 작업은 진행률을 반드시 보여 준다.
- 전문가용 옵션이 필요하면 “고급”으로 분리한다.

---

## 7. 기술 구성

| 영역 | 선택 |
|------|------|
| 앱 셸 | Electron (macOS, `titleBarStyle: hiddenInset`) |
| UI | React + Tailwind (라이트/다크) |
| 빌드 | electron-vite, electron-builder (DMG) |
| 로컬 DB | SQLite (`better-sqlite3`, WAL) |
| 시크릿 | Electron `safeStorage` (키체인) → DB에는 암호문만 |
| 스케줄 | node-cron *(수집 M2 예정)* |
| 다운로드 | yt-dlp (로컬 PATH / Homebrew) |
| 편집 | FFmpeg 9:16·워터마크 |
| AI | Claude API |
| 검색 | YouTube Data API |
| 업로드 | Instagram Graph API (Meta) |

### 프로세스 분리

- **Main**: DB, 시크릿, IPC, 외부 API·워커
- **Preload**: `window.api`만 contextBridge로 노출
- **Renderer**: UI만. 시크릿 평문은 절대 받지 않음 (저장 여부만 표시)

---

## 8. 데이터 모델 (현재)

### `workspaces`

| 컬럼 | 설명 |
|------|------|
| id | PK |
| name | 워크스페이스 이름 |
| keywords | JSON 배열 문자열 |
| interval_minutes | 수집 주기 (기본 60) |
| created_at | 생성 시각 |

### `meta_accounts`

| 컬럼 | 설명 |
|------|------|
| id | PK |
| workspace_id | FK → workspaces (CASCADE) |
| label | 표시용 이름 |
| ig_user_id | Instagram 사용자 ID |
| page_id | 연결된 Facebook 페이지 ID |
| created_at | 생성 시각 |

### `secrets`

| 컬럼 | 설명 |
|------|------|
| key | 시크릿 키 (예: `claude_api_key`, `meta_token:3`) |
| enc_value | `enc:` / `plain:` 접두 + base64 |

### `reference_items`

| 컬럼 | 설명 |
|------|------|
| id | PK |
| workspace_id | FK → workspaces (CASCADE) |
| title | 영상 제목 |
| url | 원본 주소 |
| source | `youtube` / `instagram` / `manual` |
| thumbnail_url | 썸네일 (선택) |
| author | 채널·작성자 |
| keyword | 수집에 쓰인 키워드 |
| score | Claude 추천 점수 (선택) |
| notes | 메모 |
| status | `new` / `in_pipeline` / `published` |
| created_at | 등록 시각 |

### `jobs`

| 컬럼 | 설명 |
|------|------|
| id | PK |
| workspace_id, reference_id, account_id | 연결 |
| stage | `select` → `edit` → `confirm` → `uploading` → `done` / `failed` / `cancelled` |
| edit_options | JSON (watermark_on, watermark_text, share_to_feed) |
| caption | 게시 캡션 |
| progress, message | UI 진행률·문장형 상태 |
| source_path, output_path | 로컬 파일 (`userData/jobs/{id}/`) |
| container_id, published_id | Meta 결과 |
| error | 실패 메시지 |

### 이후 추가 예정 테이블 (안)

- 업로드·댓글 이력 전용 로그
- 수집 실행 이력

---

## 9. 외부 연동·제약

| 연동 | 용도 | 비고 |
|------|------|------|
| Claude API | 추천·점수화, 댓글 문구 | 연결 테스트 지원 |
| YouTube Data API | 키워드 검색 | Google Cloud 발급, 무료 쿼터 제한 있음 |
| Meta 앱 + Graph API | 릴스 resumable 업로드 | 앱 ID/시크릿 + 계정별 토큰 |
| yt-dlp / ffmpeg | 다운로드·9:16 편집 | 로컬 Homebrew 설치 |

### 정직하게 짚은 제약

1. **IG 릴스 업로드** — 프로페셔널 계정 + Facebook 페이지 연결 + 메타 앱 검수 필요. 개인 계정 불가.
2. **IG 키워드 수집** — 공식 API 제한이 커 YouTube를 주력으로 둔다.
3. **저작권·약관** — 타인 영상 재업로드 리스크. 앱은 경고만 하고, 게시 결정은 관리자 책임.
4. **YouTube 쿼터** — 검색 호출 한도가 있으므로 수집 주기·키워드 수를 과도하게 잡지 않는다.
5. **로컬 도구** — `yt-dlp`/`ffmpeg` 미설치 시 편집 불가. IG URL 다운로드는 제한될 수 있음.

---

## 10. 개발 마일스톤

| 단계 | 내용 | 상태 |
|------|------|------|
| **M1** | 앱 뼈대, 워크스페이스 CRUD, 설정·시크릿, 메타 계정 연결, 테마 | ✅ 구현됨 |
| **M1.5** | 워크스페이스 레퍼런스 목록·직접 등록·검색 | ✅ 구현됨 |
| **M3/M4** | 선택→편집→컨펌, yt-dlp/FFmpeg, Meta resumable 게시 | ✅ 구현됨 |
| **M2** | YouTube(+IG) 수집, Claude 점수화, 스케줄러 | ⏳ 예정 |
| **M4b** | Claude 댓글 3안·게시 | ⏳ 예정 |
| **M5** | 이력 대시보드, 바이너리 번들, DMG 패키징 마무리 | ⏳ 예정 |

### 현재까지 구현된 것

- Electron + React/Tailwind 셸
- 워크스페이스·릴스 계정·레퍼런스 CRUD
- **릴스 만들기** 3단 흐름 (선택 → 편집 → 컨펌·게시)
- yt-dlp 다운로드, FFmpeg 9:16·워터마크, 진행률 IPC
- Meta Graph resumable 릴스 업로드
- Claude·YouTube·메타 앱 키 저장 및 연결 테스트
- SQLite + safeStorage 시크릿, 라이트/다크 테마

---

## 11. 성공 기준 (MVP)

1. 키워드·주기를 저장하면 설정 간격으로 후보 카드가 쌓인다. *(수집 M2)*
2. 카드에서 영상 선택 → 편집 → 미리보기·캡션 컨펌 → 릴스 게시까지 이어진다.
3. 첫 댓글 3안 중 하나를 고르거나 고쳐 게시할 수 있다. *(댓글 M4b)*
4. API 키·토큰이 화면에 다시 노출되지 않고, 저장 여부만 보인다.
5. 다운로드·편집·업로드 중 진행률과 문장형 상태가 끊기지 않고 갱신된다.

---

## 12. 결정된 사항

- 수집 출처: **YouTube + Instagram** (YouTube 주력, IG 보조)
- 업로드 대상: **Instagram 릴스** (resumable)
- 제작 흐름: **선택 → 편집 → 컨펌(미리보기·계정·캡션) → 게시** (댓글은 이후)
- 실행 환경: 관리자 단독 Mac 로컬 앱 (서버 없음)

---

## 13. 실행 방법 (개발)

```bash
# 편집·다운로드용 로컬 도구
brew install yt-dlp ffmpeg

npm install
npm run dev      # 개발
npm run build    # 빌드
npm run dist     # macOS DMG
```

앱 데이터(SQLite·작업 파일)는 Electron `userData` 경로의 `studio.db` 및 `jobs/`에 저장된다.
